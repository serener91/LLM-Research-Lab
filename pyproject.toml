[project]
name = "parkie-agentic-system"
version = "0.1.0"
description = "Agentic parking robot operations prototype"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.115.0",
  "uvicorn>=0.35.0",
  "openai-agents>=0.12.5",
  "fastmcp>=3.1.1",
  "langfuse>=4.0.6",
  "psycopg[binary]>=3.2.0",
]

[tool.uv]
package = true
